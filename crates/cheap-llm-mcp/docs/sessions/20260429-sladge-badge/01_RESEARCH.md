# Research

## Findings

- The repository is explicitly LLM-facing and provides an MCP server and CLI for
  routing subagent requests to lower-cost model providers.
- The canonical checkout was clean, but repository guidance requires feature
  work in `cheap-llm-mcp-wtrees/<topic>/`.

## Decision

Use an isolated docs branch and add the badge to the README badge block.

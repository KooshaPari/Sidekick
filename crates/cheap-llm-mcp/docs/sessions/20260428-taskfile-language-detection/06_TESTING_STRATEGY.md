# Testing Strategy

Validate the task file with:
- `task --list`
- `task build`
- `task test`
- `task lint`
- `task clean`

Because the repo is Python, the first pass should exercise the Python branch of the language
detector and confirm the commands run cleanly.

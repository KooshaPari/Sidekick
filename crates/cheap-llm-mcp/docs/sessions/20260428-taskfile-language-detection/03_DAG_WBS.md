# DAG / WBS

1. Inspect repository manifests and confirm the language.
2. Update `Taskfile.yml` to detect that language and dispatch tasks.
3. Validate the resulting task definitions.
4. Commit, push, open PR, and merge.

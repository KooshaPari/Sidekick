# Research

## Repo Evidence

The README describes Sidekick as an agent utility collection with canonical
members for `agent-user-status` and `cheap-llm-mcp`. Its integration map lists
MCP tools for user status and budget LLM routing through Minimax, Kimi, and
Fireworks. The getting-started docs describe multi-provider dispatch, agent
presence tracking, and budget LLM routing.

## Local State

The canonical `Sidekick` checkout had unrelated local changes before this pass.
The badge was prepared in an isolated worktree.

## Decision

Add the sladge badge as governance metadata and defer canonical integration
until the unrelated local changes are handled.

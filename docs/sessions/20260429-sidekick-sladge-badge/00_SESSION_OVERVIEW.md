# Session Overview

## Goal

Add the sladge governance badge to `Sidekick` because it collects agent
infrastructure utilities, including MCP presence tools and budget LLM routing.

## Outcome

- Added the `AI Slop Inside` badge to the README badge block.
- Used isolated worktree `Sidekick-wtrees/sladge-badge`.
- Left canonical `Sidekick` untouched because it already had unrelated brand,
  README, cutover, asset, submodule, and worktree changes.

## Success Criteria

- README links to `https://sladge.net`.
- Session docs record the agent/MCP/LLM rationale.
- Worktree is clean after commit.
